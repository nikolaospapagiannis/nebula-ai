services:
  # PostgreSQL Database - Port 4001
  postgres:
    image: postgres:15-alpine
    container_name: nebula-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-nebula}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nebula123}
      POSTGRES_DB: ${POSTGRES_DB:-nebula_db}
    ports:
      - '4001:5432'
    volumes:
      - nebula_postgres_data:/var/lib/postgresql/data
    networks:
      - nebula-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nebula']
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache - Port 4002
  redis:
    image: redis:7-alpine
    container_name: nebula-redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis123}
    ports:
      - '4002:6379'
    volumes:
      - nebula_redis_data:/data
    networks:
      - nebula-network
    healthcheck:
      test: ['CMD', 'redis-cli -a redis123', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  # Elasticsearch for Search - Port 4003
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: nebula-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - 'ES_JAVA_OPTS=-Xms256m -Xmx256m'
    ports:
      - '4003:9200'
    volumes:
      - nebula_elasticsearch_data:/usr/share/elasticsearch/data
    networks:
      - nebula-network
    healthcheck:
      test: ['CMD-SHELL', 'curl -f http://localhost:9200/_cluster/health || exit 1']
      interval: 30s
      timeout: 10s
      retries: 5

  # RabbitMQ for Message Queue - Port 4004 (AMQP), 4005 (Management)
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: nebula-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-nebula}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-nebula123}
    ports:
      - '4004:5672'
      - '4005:15672'
    volumes:
      - nebula_rabbitmq_data:/var/lib/rabbitmq
    networks:
      - nebula-network
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', 'ping']
      interval: 30s
      timeout: 10s
      retries: 5

  # MinIO for S3-compatible Storage - Port 4006 (API), 4007 (Console)
  minio:
    image: minio/minio:latest
    container_name: nebula-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_USER:-nebula}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD:-minio123456}
    ports:
      - '4006:9000'
      - '4007:9001'
    volumes:
      - nebula_minio_data:/data
    networks:
      - nebula-network
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 30s
      timeout: 20s
      retries: 3

networks:
  nebula-network:
    driver: bridge

volumes:
  nebula_postgres_data:
  nebula_redis_data:
  nebula_elasticsearch_data:
  nebula_rabbitmq_data:
  nebula_minio_data:
