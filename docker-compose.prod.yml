version: '3.9'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: fireff-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-fireflies}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-fireflies123}
      POSTGRES_DB: ${POSTGRES_DB:-fireflies_db}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - fireff-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U fireflies']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: fireff-redis
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis123}
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    networks:
      - fireff-network
    healthcheck:
      test: ['CMD', 'redis-cli', '--auth', '${REDIS_PASSWORD:-redis123}', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # MongoDB for Transcripts
  mongodb:
    image: mongo:7
    container_name: fireff-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-fireflies}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-mongo123}
      MONGO_INITDB_DATABASE: ${MONGO_DB:-fireflies_transcripts}
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    networks:
      - fireff-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # MinIO for S3-compatible Storage
  minio:
    image: minio/minio:latest
    container_name: fireff-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_USER:-fireflies}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD:-minio123456}
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data
    networks:
      - fireff-network
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 30s
      timeout: 20s
      retries: 3
    restart: unless-stopped

  # API Service
  api:
    build:
      context: .
      dockerfile: ./apps/api/Dockerfile
    container_name: fireff-api
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 4000
      DATABASE_URL: postgresql://${POSTGRES_USER:-fireflies}:${POSTGRES_PASSWORD:-fireflies123}@postgres:5432/${POSTGRES_DB:-fireflies_db}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis123}
      MONGODB_URL: mongodb://${MONGO_USER:-fireflies}:${MONGO_PASSWORD:-mongo123}@mongodb:27017/${MONGO_DB:-fireflies_transcripts}?authSource=admin
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: ${MINIO_USER:-fireflies}
      S3_SECRET_KEY: ${MINIO_PASSWORD:-minio123456}
      JWT_SECRET: ${JWT_SECRET:-change-this-secret-in-production}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-change-this-refresh-secret}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-change-this-32-char-encryption-key}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-your-openai-key}
    ports:
      - '4000:4000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    networks:
      - fireff-network
    restart: unless-stopped

  # Web Frontend
  web:
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost:4000
        NEXT_PUBLIC_WS_URL: ws://localhost:5000
    container_name: fireff-web
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      NEXT_PUBLIC_API_URL: http://localhost:4000
      NEXT_PUBLIC_WS_URL: ws://localhost:5000
    ports:
      - '3000:3000'
    depends_on:
      - api
    networks:
      - fireff-network
    restart: unless-stopped

  # AI Service (Python)
  ai-service:
    build:
      context: .
      dockerfile: ./apps/ai-service/Dockerfile
    container_name: fireff-ai-service
    environment:
      PYTHON_ENV: ${PYTHON_ENV:-production}
      PORT: 8000
      DATABASE_URL: postgresql://${POSTGRES_USER:-fireflies}:${POSTGRES_PASSWORD:-fireflies123}@postgres:5432/${POSTGRES_DB:-fireflies_db}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis123}
      MONGODB_URL: mongodb://${MONGO_USER:-fireflies}:${MONGO_PASSWORD:-mongo123}@mongodb:27017/${MONGO_DB:-fireflies_transcripts}?authSource=admin
      OPENAI_API_KEY: ${OPENAI_API_KEY:-your-openai-key}
      WHISPER_MODEL: ${WHISPER_MODEL:-base}
    ports:
      - '8000:8000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    networks:
      - fireff-network
    restart: unless-stopped

  # Real-time WebSocket Service
  realtime-service:
    build:
      context: .
      dockerfile: ./apps/realtime-service/Dockerfile
    container_name: fireff-realtime
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3002
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis123}
    ports:
      - '3002:3002'
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - fireff-network
    restart: unless-stopped

networks:
  fireff-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  mongodb_data:
    driver: local
  minio_data:
    driver: local
